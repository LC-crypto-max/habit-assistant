# AGENTS.md

本项目是 Java / Spring Boot 个人兴趣助手，包含 Codex Data Agent 模块，用于在用户本人授权的前提下，处理非敏感访问行为数据，生成兴趣画像和每日推荐。

## 核心原则

1. 本地优先。
2. 用户授权。
3. 最小化采集。
4. 默认脱敏。
5. 不保存账号密码。
6. 不保存 Cookie、Token、Session。
7. 不读取聊天记录、私信、通讯录、支付记录。
8. 不绕过验证码。
9. 不破解接口。
10. 不批量爬取受保护内容。

## 允许处理的数据

* App 使用概况：App 名称、包名、使用时长、打开次数；
* 浏览器历史：URL、标题、访问时间、访问次数；
* 用户主动提供的公开链接；
* 用户授权目录下的本地笔记；
* 公开网页的 title、description、keywords。

## 禁止处理的数据

* Cookie；
* Token；
* Session；
* Authorization Header；
* 账号密码；
* 微信聊天数据库；
* 微信聊天记录；
* 私信；
* 通讯录；
* 支付记录；
* 验证码；
* 平台私有接口；
* 他人隐私数据。

## 开发要求

* 所有采集器必须经过 DataPolicyChecker。
* 所有输出必须经过 PrivacySanitizer。
* 日志中不得打印敏感原文。
* 新增采集器必须新增测试。
* 新增功能必须更新文档。
* 第一版优先使用模拟数据和 JSON 文件存储跑通完整链路。
* 推荐模块先生成推荐查询词，不直接批量抓取平台内容。

## 测试命令

优先尝试：

```powershell
./mvnw test
```

如果没有 mvnw，则使用：

```powershell
mvn test
```
