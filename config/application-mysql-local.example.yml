# 复制本文件为 application-mysql-local.yml，再填写你本机的 MySQL 信息。
# application-mysql-local.yml 已被 .gitignore 忽略，不会进入版本库。
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/habit_assistant?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&connectionCollation=utf8mb4_unicode_ci&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: 361961
